import { View, Text, ScrollView } from 'react-native'
import React from 'react'

export default function HourForecast({ data }) {
    return (
        <ScrollView>
            <View>
                <Text>HourForecast</Text>
            </View>
        </ScrollView>
    )
}
